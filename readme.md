# Подсистема приоритетного управления видеонаблюдением (ППУВ)

## 1. Общая концепция

### 1.1. Бизнес-контекст

* Подсистема приоритетного управления видеонаблюдением (ППУВ) реализуется в **федеративной** архитектуре: **отдельная платформа СРВИ** (контур уполномоченного ведомства) и **одна или несколько региональных платформ**.
* <span style="color:blue;">Даже минимальная схема из двух платформ считается федерацией. Реализация не должна строиться как временная "нефедеративная" полумера с последующим переносом: уже в первой поставке нужно закладывать взаимодействие независимых платформ через явные межсистемные интерфейсы.</span>
* Ключевое требование:
  * региональные площадки **не сводятся в единую сеть**;
  * уполномоченному ведомству предоставляется **приоритетный бесшовный доступ** к системам видеонаблюдения.

### 1.2. Принципы архитектуры

* **Две (и более) независимые платформы**: у СРВИ и региональной системы — **свой фронтенд и свой бэкенд**. Пользователь, залогиненный в одной системе, **не выполняет операции** в другой напрямую.
* <span style="color:blue;">Пользователь СРВИ никогда не заходит в региональную платформу под пользовательским аккаунтом. Все действия выполняются из UI СРВИ, а в регион уходят только доверенные S2S-команды.</span>
* **Одинаковый состав платформ**. СРВИ — это **не урезанная** копия; по набору сервисов платформы эквивалентны. Отличие будет в сценариях использования.

  Это также означает, что на СРВИ могут быть свои камеры, аналитики, уведомления и т.п., которые доступны только со стороны платформы СРВИ.
* **СРВИ — точка управления ППУВ**. Блокировки, перехват, журнал блокировок, аудит операций ППУВ будут находиться **на стороне СРВИ**. На региональной платформе **нет** операторского ППУВ-функционала (супер-оператор, перехват чужих блокировок и т.п.). Технически элементы ППУВ могут быть включены, но должны пресекаться на управленческом уровне.
* <span style="color:blue;">Главенство СРВИ проявляется в спорных ситуациях ППУВ: блокировка камеры, блокировка архива, резервирование, выборочная запись и перехват PTZ. В остальных доменных сценариях региональная система продолжает жить как самостоятельная платформа.</span>
* **Интеграция через RSM, не через EM**. Взаимодействие между платформами — **отдельные S2S-интеграции** (расширенный RSM, прокси-команды), а не "склейка" внутренних сервисов одной инсталляции.
* **Масштабирование на N регионов**. На текущий момент может быть только одна региональная система, но закладываем возможность нескольких.

  При включении новых платформ возможен вариант как добавления новой региональной структуры в текущую систему, так и разворачивания новой связки СРВИ и региональной платформы.
* **Признак происхождения камеры**. В UI и в данных обязательно различать: локальные камеры СРВИ, камеры региональной системы A, камеры региональной системы B и т.д.
* <span style="color:blue;">Даже если в первой поставке подключен один регион, признак источника, `externalId` камеры и идентификатор региональной инсталляции обязательны. Это критерий приемки бэкенд-архитектуры, а не опциональная UI-метка.</span>
* **Live-отображение**:
  * для гражданского пользователя должно быть всё максимально скрыто, или казаться, что камера не работает или не пишется архив;
  * для пользователя СРВИ должно быть визуально понятно, откуда получены камеры, и должны быть видны заблокированные фрагменты на таймлайне.
* <span style="color:red;">Для оператора ППУВ красная рамка/цветовой статус камеры недостаточны: заблокированная камера должна иметь явное визуальное объяснение состояния (например, замок, подпись "Заблокирована", таймер или иной однозначный индикатор), чтобы её не путали с аварией или недоступностью потока.</span>
* **События аналитик и нотификации** просматриваются для каждой платформы отдельно. Пользователь СРВИ не видит аналитики на региональной платформе. Пользователь СРВИ видит только события по локальным камерам СРВИ.
* <span style="color:blue;">При блокировке архива или камеры региональная платформа должна скрывать от гражданских пользователей не только архивные сегменты, но и события аналитик/нотификации за соответствующий интервал. Для СРВИ эти региональные события всё равно не агрегируются.</span>
* **Аудит.** Региональный пользователь не видит, что сделал оператор СРВИ, данные никак **не отображаются в аудите** региональной платформы. Но в аудите СРВИ все действия сохраняются. Пользователь СРВИ **не видит аудит** на региональной платформе.
* **Журнал блокировок** отображается только на платформе СРВИ, с учётом всех необходимых функций (сортировки, фильтрации, разблокировки камеры).
* <span style="color:red;">Журнал блокировок — это не только лог событий, а список текущих и исторических состояний lease: одна блокировка отображается одной строкой со статусом, временем начала/окончания, владельцем, тем, кто снял блокировку, и доступными действиями. Для активных блокировок в журнале нужны действия "Снять" и "Продлить".</span>
* Одна лицензия по работе с ППУВ распространяется на обе платформы. Вопрос доступов решается через управленческое воздействие.
* Конфликты решаются **по каждой камере отдельно**, а не "вся зона целиком чья-то или ничья".

### 1.3. Типы камер и данных

```text
┌─────────────────────────────────────┐     S2S (RSM)      ┌──────────────────────────────────┐
│           Платформа СРВИ            │ ◄──────────────► │   Региональная платформа (×N)    │
│  • локальные камеры (свой контур)   │                  │  • муниципальные камеры          │
│  • проекции камер из регионов       │                  │  • медиастримеры + Nimble        │
│  • медиастримеры для блокировок /   │                  │  • приём команд блокировки       │
│    резервирования                   │                  │    от СРВИ                       │
│  • RFM для долговременных копий     │                  │  • логическое скрытие архива     │
└─────────────────────────────────────┘                  └──────────────────────────────────┘
```

<span style="color:blue;">Проекции региональных камер должны быть доступны в каталоге СРВИ заранее или создаваться управляемо при первом действии. В обоих вариантах СРВИ хранит собственный `resourceId`, внешний `externalId` камеры региона и `regionalInstallationId`.</span>

### 1.4. Операции и сущности ППУВ

* **Важно!**
  * Все операции ППУВ инициируются на платформе СРВИ.
  * Сохранённые элементы архива в RFM должны отображаться на таймлайне камеры.
  * <span style="color:blue;">Оператор СРВИ смотрит федеративную камеру через камеру/плеер СРВИ. Плеер должен уметь собирать единый таймлайн из регионального медиастримера, стримера СРВИ и RFM, не заставляя пользователя переходить в региональную систему.</span>

#### Блокировка камеры (спецрежим)

* Спецпользователь **скрывает реальную картину** с камеры от гражданских на время операции: вместо live — нерабочий поток (для одиночной камеры) или заглушка/подмена (для раскладки); архив за период блокировки для них недоступен, так же как события. Для пользователя ППУВ поток и запись остаются реальными. Снимается по **TTL** или вручную.
* Блокировка камеры затрагивает блокировку архива.
* <span style="color:red;">Пользовательское действие "блокировка потока/камеры" не должно автоматически создавать отдельный PTZ-lease. Оно лишь косвенно делает управление PTZ недоступным для гражданского пользователя, потому что поток/джойстик для него не открываются.</span>
* <span style="color:red;">Если блокировка потока уже включена, UI блокировки PTZ для этой камеры должен быть disabled/недоступен и должен объяснять, что управление уже недоступно из-за блокировки потока. Отдельную PTZ-блокировку поверх уже включённой блокировки потока накладывать не требуется.</span>
* <span style="color:blue;">На региональной платформе при блокировке применяется логическое скрытие для гражданского контура; данные региона не удаляются. Одновременно СРВИ включает собственную запись/копию нужного потока на своём медиастримере для периода блокировки.</span>
* <span style="color:blue;">После завершения блокировки заблокированный фрагмент должен быть зафиксирован в RFM СРВИ для долговременного хранения. Политика хранения настраиваемая; Nimble/медиастример не считается надёжным долговременным хранилищем, так как архив может быть очищен штатной ретенцией.</span>
* <span style="color:red;">Максимальный TTL ручной блокировки потока и ручной блокировки PTZ по умолчанию — 3 дня суммарно независимо от единицы ввода (минуты/часы/дни). Значение должно быть вынесено в env/config, чтобы для конкретной инсталляции его можно было изменить без пересборки фронта.</span>

#### Перехват PTZ (PTZ preemption)

* Спецпользователь ППУВ **принудительно забирает управление** поворотной камерой у текущего пользователя за счёт более высокого приоритета. Live и архив **не отключаются**; предыдущий оператор получает уведомление.
* <span style="color:red;">В пользовательском интерфейсе эту операцию следует называть "Блокировка PTZ", а не "Перехват управления": оператор задаёт период, в течение которого другие пользователи не могут поворачивать камеру, но live и архив остаются доступными. "Перехват" остаётся техническим механизмом сброса/вытеснения текущей PTZ-сессии при выдаче более приоритетного управления.</span>
* <span style="color:red;">Если сначала наложена блокировка PTZ, оператор ППУВ может позже наложить блокировку потока на ту же камеру. При этом PTZ-lease и Spoofing/stream-lock являются разными состояниями с собственными TTL, но UI должен не провоцировать пользователя создавать бессмысленную PTZ-блокировку после уже включённой блокировки потока.</span>

#### <span style="color:dimgray;">*Блокировка положения камеры (PTZ-lock) — будущая итерация*</span>

* <span style="color:dimgray;">*Оператор* ***закрепляет за собой управление*** *PTZ на камеру или группу. Пока блокировка активна, другие не могут поворачивать камеру; просмотр live и архива для всех остаётся доступным.*</span>
* <span style="color:red;">Уточнение: базовый вариант этой сущности переносится в текущий scope под названием "Блокировка PTZ". Будущей итерацией остаются расширения вроде расписаний, более сложных правил фиксации положения и дополнительных UX-сценариев.</span>

#### Блокировка архива (Archive-lock) и изъятие

* Возможны 2 варианта блокировки архива:
  * **Логическая блокировка:** оператор закрывает доступ к записям за выбранный период. На таймлайне у гражданских — "дыра"; live не затрагивается. Блок **не снимается автоматически**. Скрытие **обратимо** при снятии lock.
  * **Физическая блокировка:** оператор **накладывает логическую блокировку** на фрагмент гражданского контура и **копирует фрагмент архива** с регионального Nimble/медиастримера в RFM СРВИ.

    Физическая блокировка выполняется при блокировке камеры, а также может быть выбрана при блокировке выделенного фрагмента.

    Блок снимается стандартным образом.
* <span style="color:blue;">Для просмотра полного архива федеративной камеры СРВИ не обязан предварительно дублировать весь архив. Обычные интервалы могут читаться по подписанной внешней ссылке/прокси с регионального медиастримера, а заблокированные, зарезервированные и изъятые интервалы — из источников СРВИ/RFM.</span>

#### Резервирование архива

* Оператор **заранее** подключает федеративную камеру к **параллельной записи** на медиастример СРВИ, не скрывая поток от гражданских. Это необходимо, чтобы при отказе региональной площадки сохранились live и архив в контуре СРВИ.
* <span style="color:blue;">Резервирование может быть срочным или бессрочным до ручного снятия. В отличие от блокировки камеры, оно не мешает гражданскому контуру и не должно скрывать live/архив/события у региона.</span>
* <span style="color:blue;">Резервирование означает подключение потока к контуру СРВИ, а не только последующую выгрузку файла: СРВИ должен иметь собственную запись на период резервирования.</span>
* <span style="color:red;">Резервирование не добавляется третьей строкой в модальное окно блокировок потока/PTZ. Для него нужен отдельный элемент управления в карточке камеры или плеере; точное место в UI фиксируется отдельным UX-решением.</span>

#### Выборочная запись архива

* Оператор **сохраняет копию** уже существующего исторического фрагмента (файл из Nimble/медиастримера региона) в RFM СРВИ.
* <span style="color:blue;">Оператор выделяет интервал на таймлайне СРВИ; система получает файл из регионального архива через S2S/служебный API и сохраняет его в RFM без удаления оригинала и без блокировки live.</span>

#### Взаимовлияние сущностей

| Сущность | Спецрежим | <span style="color:dimgray;">*PTZ-lock (будущее)*</span> | Перехват PTZ | Archive-lock |
| --- | --- | --- | --- | --- |
| Блокировка камеры | — | Затрагивает | Затрагивает | Затрагивает (авто) |
| <span style="color:dimgray;">*PTZ-lock*</span> | Не влияет | — | Затрагивает | Не влияет |
| Перехват PTZ | Не влияет | Затрагивает | — | Не влияет |
| Archive-lock | Не влияет | <span style="color:dimgray;">Не влияет</span> | Не влияет | — |
| <span style="color:blue;">Резервирование архива</span> | <span style="color:blue;">Не скрывает, но может совпадать по камере</span> | <span style="color:blue;">Не влияет</span> | <span style="color:blue;">Не влияет</span> | <span style="color:blue;">Не влияет</span> |

<span style="color:red;">Уточнение по влиянию блокировки потока и PTZ: блокировка потока делает PTZ недоступным как побочный эффект отсутствия доступного гражданскому пользователю потока/джойстика, но не создаёт отдельную запись PTZ-lock. Блокировка PTZ может существовать сама по себе и не скрывает live/архив.</span>

### 1.5. Пользователи

* В федеративной схеме роли привязаны к платформе, на которой пользователь аутентифицирован. ППУВ-операции доступны только на СРВИ; региональная платформа не предоставляет операторский ППУВ-функционал (супер-оператор, перехват чужих блокировок и т.п.).
* Возможные пользователи:
  * **Спецпользователь (ППУВ) — контур СРВИ:** наивысший приоритет в рамках СРВИ. Перехват PTZ, блокировка камер и архива (в т.ч. проекций регионов), скрытие действий от регионального аудита, работа с изъятым архивом в RFM, журнал блокировок. Видит события аналитики и нотификации только по локальным камерам СРВИ; аналитика региональных платформ для него недоступна.
  * **Гражданский оператор — региональная платформа:** базовый приоритет. Управляет муниципальными камерами, пока RSM не применил ограничение со стороны СРВИ. При блокировке/перехвате теряет доступ согласно §1.6; **не видит** в аудите действий оператора СРВИ.
  * **Гражданский оператор — СРВИ (локальные камеры):** штатная работа с камерами контура СРВИ без ППУВ-приоритета; при конфликте с ППУВ — те же правила вытеснения.
  * **Системный администратор — на любой из платформ:** настраивает уровни доступа в Resource Management **своей** платформы, но **не получает** оперативного приоритета над ППУВ в реальном времени.

**Особенности:**

* Для пользователей с **одинаковым приоритетом** — право первого (first-come-first-served).
* Снять блокировку может **только владелец** или пользователь со спецправом перехвата/администрирования ППУВ на СРВИ (`admin:ppuv` / `ppuv:admin`).
* <span style="color:dimgray;">*На будущее: числовые приоритеты для промежуточных ролей (30 / 50 / 70) с попарным сравнением при конфликтах.*</span>

### 1.6. Поведение системы в разных ситуациях

| # | Что проверяем | Гражданский на региональной платформе | Оператор ППУВ на СРВИ |
| --- | --- | --- | --- |
|  | **Блокировка камеры** |  |  |
| 1 | Live-поток | ❌ Заглушка / "камера не работает" | ✅ Реальный поток |
| 2 | Камера в раскладке | ❌ Подмена / "не работает" | ✅ С указанием источника (регион) |
| 3 | Архив до спецрежима | ✅ На региональном Nimble/медиастримере | ✅ Через таймлайн СРВИ: региональный источник + свои копии при наличии |
| 4 | Архив за время спецоперации | ❌ | ✅ Дубль на платформе СРВИ; <span style="color:blue;">после завершения — фрагмент в RFM для долгого хранения</span> |
| 5 | Управление PTZ | ❌ | ✅ автор блокировки; ❌ другие пользователи ППУВ* <span style="color:red;">Отдельный PTZ-lease при блокировке потока не создаётся.</span> |
| 6 | События аналитики за период | ❌ (на регионе скрыты) | ❌ по региональной камере** |
| 7 | Нотификации за период | ❌ (на регионе скрыты) | ❌ по региональной камере** |
|  | **Блокировка камеры снята** |  |  |
| 8 | Live-поток | ✅ | ✅ |
| 9 | Архив за период блокировки | ❌ | ✅ |
| 10 | События за период спецоперации | ❌ | ❌ |
|  | **Перехват PTZ** |  |  |
| 11 | Live-поток | ✅ | ✅ |
| 12 | Архив | ✅ | ✅ |
| 13 | Управление PTZ | ❌ (сессия сброшена через S2S) | ✅ автор; ❌ другие пользователи ППУВ |
| 14 | Уведомление о перехвате | ✅ | ✅ другим ППУВ при конфликте |
| 15 | <span style="color:red;">Название операции в UI</span> | <span style="color:red;">Видит отключение управления</span> | <span style="color:red;">"Блокировка PTZ"; "перехват" — техническое вытеснение текущей сессии</span> |
|  | **Archive-lock активен** |  |  |
| 25 | Live сейчас | ✅ | ✅ |
| 26 | Архив вне интервала | ✅ | ✅ |
| 27 | Архив внутри интервала | ❌ | ✅ |
| 28 | HLS / сегменты в интервале | ❌ | ✅ |
| 29 | Экспорт / download интервала | ❌ | ✅ любому пользователю ППУВ с правом |
| 30 | Управление PTZ | ✅ | ✅ |
| 31 | События аналитики в интервале | ❌ | ❌ по региональной камере** |
| 32 | Нотификации в интервале | ❌ | ❌ по региональной камере** |
|  | **Archive-lock снят** |  |  |
| 33 | Архив в бывшем интервале | ✅ | ✅ |
|  | **После физической блокировки / изъятия** |  |  |
| 34 | Архив в заблокированном интервале | ❌ заблокирован | ✅ в RFM (при наличии копии) и на таймлайне камеры |
|  | <span style="color:blue;">**Резервирование архива**</span> |  |  |
| 35 | <span style="color:blue;">Live и архив</span> | <span style="color:blue;">✅ Без изменений</span> | <span style="color:blue;">✅ Собственная запись/резерв в контуре СРВИ</span> |
| 36 | <span style="color:blue;">События и нотификации</span> | <span style="color:blue;">✅ Без изменений</span> | <span style="color:blue;">❌ региональные события не агрегируются</span> |

\* Если один ППУВ управлял камерой, а второй включил спецрежим, у первого пропадает PTZ; управление доступно только автору блокировки спецрежима.

\** Аналитика и нотификации регионов для пользователя СРВИ не агрегируются; ППУВ видит аналитику только по локальным камерам СРВИ.

### 1.7. Технические нюансы

* RSM — сервис, хранящий состояние блокировок (что и кем занято) и координирующий сквозные операции. При нажатии "заблокировать камеру" он проверяет права, по таблице сопоставления находит нужный региональный контур, отправляет команду региональному RSM, запускает запись архива на стример СРВИ и фиксирует событие в журнале. Всё это скрыто от оператора за одним действием, а сам RSM также обслуживает импорт камер, блокировку архива и перехват PTZ.
* В каталоге СРВИ камеры из разных регионов выглядят одинаково для оператора, но внутри системы это разные сущности: при импорте создаётся собственная запись (`resourceId`), к которой привязываются `externalId` камеры в регионе и идентификатор региональной инсталляции. Эта таблица сопоставления нужна RSM, чтобы понимать, в какой именно региональный контур отправлять команду блокировки.
* Региональная система остаётся закрытой для обычных пользователей, но предоставляет контролируемый server-to-server API — служебный вход исключительно для доверенного контура СРВИ, через который RSM отправляет команды блокировки, разблокировки и работы с архивом. Защита типовая для межсистемной интеграции: ключи, client id, проверка подписи и разграничение по приоритетам.
* Пока камера не в спецрежиме, архив пишется на региональных медиастримерах. При блокировке гражданским показывается заглушка/подмена, их архив за этот период закрыт, а запись нужного интервала ведётся в контуре СРВИ.
* <span style="color:blue;">Для просмотра незаблокированных исторических интервалов допустим сценарий, где СРВИ получает от регионального медиастримера подписанную внешнюю ссылку или проксируемый DVR/HLS-ресурс. Это решает просмотр без массового дублирования всех архивов.</span>
* <span style="color:blue;">Для заблокированных, зарезервированных и изъятых фрагментов источник данных для плеера СРВИ — медиастример СРВИ и/или RFM. На уровне фронта это должен выглядеть как единый таймлайн камеры, даже если физически куски лежат в разных местах.</span>
* RFM в контуре СРВИ нужен, когда оператор не просто скрывает архив на время, а забирает насовсем или сохраняет копию прошлого фрагмента.
* Нужно учесть, что Nimble не умеет "перелить" архив целиком с сервера на сервер как единый поток — только выгрузить файл. RFM как раз место, куда этот файл кладут надолго.
* <span style="color:blue;">После окончания блокировки камеры нужен системный триггер/процесс, который сохраняет соответствующий фрагмент в RFM. Иначе фрагмент может быть потерян после очистки архива медиастримера.</span>
* <span style="color:blue;">Автоматическая замена заблокированной камеры случайной онлайн-камерой в раскладке является требованием ТЗ, но должна поставляться под feature flag и по умолчанию быть выключенной. Статичный пресет/картинка не закрывает это требование как полная замена, но может использоваться для одиночного просмотра "камера не работает".</span>
* <span style="color:red;">RSM/API блокировок должны быть пригодны для будущего сервиса расписаний: расписание не требует отдельной логики внутри RSM, а должно вызывать те же acquire/release endpoints, что и ручные операции. В текущем scope расписание блокировок не реализуется, но API не должен этому препятствовать.</span>
* <span style="color:red;">Сервис/конфигурация блокировок должен отдавать фронту максимальный TTL блокировки (по умолчанию 3 дня). Фронт валидирует ввод до отправки, бэкенд повторно валидирует TTL и возвращает понятную ошибку при превышении.</span>
* <span style="color:red;">Для карты статус блокировки запрашивается и отображается только для одиночных камер, которые реально присутствуют в ответе карты. Кластеры не должны подсвечиваться как заблокированные, потому что в них нет полного списка cameraId; при раскрытии кластера/появлении одиночных камер замок показывается на конкретных камерах.</span>
* <span style="color:red;">Массовая блокировка по лассо должна передавать на бэкенд геометрию области; бэкенд сам определяет попавшие камеры и применяет лимиты. Нельзя требовать от фронта перечислить cameraId внутри кластеров.</span>
* <span style="color:red;">Для фильтра "заблокированные камеры" желательно сохранять/проецировать актуальный lock-state в свойства камеры или предоставлять совместимый агрегирующий endpoint, чтобы фильтр мог работать вместе с существующими фильтрами карты/списка.</span>
* <span style="color:red;">При федерации СРВИ должен держать актуальный слепок внешних камер в своём каталоге: название, координаты, источник, внешние идентификаторы, признаки доступности и необходимые атрибуты для фильтрации. Изменения в регионе должны синхронизироваться в СРВИ, а поток/архив при этом остаются физически в региональном контуре до резервирования или блокировки.</span>
* <span style="color:red;">Для тегов, иерархий и поиска по нескольким источникам нужно учитывать коллизии имён и разные id. Минимально в первой итерации источник камеры должен быть частью данных фильтрации; для нескольких регионов теги/иерархии должны быть namespace-aware или отображаться в разрезе источника.</span>
* <span style="color:red;">Finder/поиск на карте должен рассматриваться как будущий потребитель тех же фильтров по источнику и статусу блокировки; детальная реализация Finder не входит в текущий scope, но новые поля не должны ломать его последующую интеграцию.</span>

### 1.8. Чек-лист приемки федеративной части

<span style="color:blue;">Этот раздел добавлен по итогам встречи как компактный список решений, которые нужно видеть при разработке и приемке.</span>

* <span style="color:blue;">Камеры региона видны в СРВИ как проекции с собственным `resourceId`, `externalId` и `regionalInstallationId`.</span>
* <span style="color:blue;">Все ППУВ-действия над региональными камерами запускаются из UI СРВИ; пользователь СРВИ не логинится в региональную платформу.</span>
* <span style="color:blue;">Блокировка камеры на регионе скрывает live, архив, события и нотификации от гражданских пользователей региона, но не пишет следы операции в гражданский аудит региона.</span>
* <span style="color:blue;">Период блокировки доступен оператору СРВИ на таймлайне камеры и сохраняется в RFM для долговременного просмотра.</span>
* <span style="color:blue;">Полный таймлайн федеративной камеры в СРВИ может состоять из региональных DVR/HLS-ссылок, собственного архива СРВИ и RFM-фрагментов.</span>
* <span style="color:blue;">Резервирование архива работает без скрытия гражданского live/архива и может быть бессрочным до ручного снятия.</span>
* <span style="color:blue;">Выборочная запись исторического интервала сохраняет файл в RFM без удаления оригинала с регионального медиастримера.</span>
* <span style="color:blue;">Для раскладок случайная подмена заблокированной камеры реализуется как отключаемая feature flag.</span>
* <span style="color:red;">Ручные блокировки потока и PTZ имеют максимальный TTL 3 дня по умолчанию; лимит конфигурируется через окружение и проверяется и на фронте, и на бэке.</span>
* <span style="color:red;">В UI операция запрета поворота называется "Блокировка PTZ"; "перехват" не используется как основное пользовательское название.</span>
* <span style="color:red;">Журнал блокировок показывает состояния lease и позволяет снять или продлить активную блокировку.</span>
* <span style="color:red;">На карте замок отображается только на одиночных камерах; кластеры не получают агрегированный признак блокировки.</span>

---

## 2. Бизнес-требования (User Stories)

### 2.1. Блокировки и управление ресурсами

* **US.PPUV.1.1.** Как пользователь ППУВ на платформе СРВИ, я хочу включать режим блокировки/спецоперации на камере через тумблер с выбором периода блокировки и повторным нажатием для снятия, с отображением состояния на иконке камеры, чтобы скрывать реальную картину от гражданских пользователей, оставаясь с доступом к настоящему потоку и архиву.
* **US.PPUV.1.2.** Как пользователь ППУВ на платформе СРВИ, я хочу видеть при одиночном просмотре камеры на карте, в списке и в галерее иконку блокировки и время до окончания блокировки камеры, чтобы понимать, какие камеры заняты и когда освободятся.
* **US.PPUV.1.3.** Как гражданский оператор региональной платформы, я хочу, чтобы камера в режиме спецоперации незаметно исчезала из моей раскладки и заменялась другой, а в одиночном просмотре показывала "неработающую" камеру, чтобы не привлекать внимание к проводимой операции.

  <span style="color:blue;">Важно: случайная онлайн-подмена в раскладке должна быть реализована как feature flag и по умолчанию отключена.</span>
* **US.PPUV.1.4.** Как пользователь ППУВ на платформе СРВИ, я хочу перехватывать управление поворотной камерой (в том числе проекцией региональной площадки) с блокировкой других пользователей, чтобы вести слежение за объектом без помех.
  <span style="color:red;">Уточнение: в UI это действие называется "Блокировка PTZ". Пользователь выбирает срок блокировки PTZ; live и архив при этом не скрываются.</span>
* **US.PPUV.1.5.** Как пользователь ППУВ на платформе СРВИ, я хочу выделить на карте область (лассо) и заблокировать камеры внутри неё (не более 50 за операцию), выбрав длительность из предложенных вариантов, чтобы оперативно закрепить зону наблюдения; при пересечении с областью другого пользователя система должна однозначно определить владельца каждой камеры и уведомить меня о частичном успехе или отказе.
  <span style="color:red;">Массовое действие по области должно поддерживать как блокировку потока, так и блокировку PTZ, чтобы можно было зафиксировать группу PTZ-камер без скрытия live/архива.</span>
* **US.PPUV.1.6.** Как пользователь ППУВ на платформе СРВИ, я хочу выделить ранее заблокированную мной область на карте и снять блокировки со всех своих камер в ней, чтобы освободить зону для других пользователей.
* **US.PPUV.1.7.** Как гражданский оператор региональной платформы, я хочу получать явное уведомление и отключение PTZ-управления при перехвате камеры оператором ППУВ с платформы СРВИ, чтобы не продолжать бесполезные команды.
* <span style="color:blue;">**US.PPUV.1.8.** Как пользователь ППУВ на платформе СРВИ, я хочу видеть все доступные локальные и региональные камеры в едином каталоге/карте СРВИ с фильтром по источнику, чтобы работать с федеративной системой из одного интерфейса.</span>
* <span style="color:red;">**US.PPUV.1.9.** Как пользователь ППУВ на платформе СРВИ, я хочу фильтровать камеры по признаку активной блокировки и по источнику, чтобы быстро находить занятые камеры на карте, в списке и в галерее.</span>
* <span style="color:red;">**US.PPUV.1.10.** Как пользователь ППУВ на платформе СРВИ, я хочу видеть на карточке/плеере заблокированной камеры явную подпись или иконку состояния, чтобы отличать блокировку ППУВ от технической неисправности.</span>

### 2.2. Скрытие архива

* **US.PPUV.2.1.** Как пользователь ППУВ на платформе СРВИ, я хочу скрывать выбранный интервал архива камеры (в том числе проекции региональной площадки) от гражданских пользователей, чтобы ограничить доступ к чувствительным записям без физического изъятия.
* **US.PPUV.2.2.** Как пользователь ППУВ на платформе СРВИ, я хочу дублировать фрагмент архива в защищённый контур СРВИ (RFM) без удаления с регионального Nimble, чтобы выбранный при блокировании фрагмент сохранился на длительный период времени.
* **US.PPUV.2.3.** Как пользователь ППУВ на платформе СРВИ, я хочу иметь возможность вернуть скрытый интервал архива, чтобы при ошибочном наложении блокировки была возможность исправить ошибку.
* **US.PPUV.2.4.** Как пользователь ППУВ на платформе СРВИ, я хочу заранее включить резервирование архива для федеративной камеры (параллельная запись на медиастример СРВИ без скрытия live от гражданских), чтобы при отказе региональной площадки сохранились live и свой архив в контуре СРВИ.
* <span style="color:blue;">Резервирование может быть задано на период или включено бессрочно до ручного снятия.</span>
* **US.PPUV.2.5.** Как пользователь ППУВ на платформе СРВИ, я хочу сохранить копию уже записанного исторического фрагмента архива (файл из Nimble региона) в RFM СРВИ без блокировки live и без удаления записи с регионального медиастримера, чтобы иметь долгосрочную копию в защищённом контуре, не лишая гражданских доступа к оригиналу.
* <span style="color:blue;">**US.PPUV.2.6.** Как пользователь ППУВ на платформе СРВИ, я хочу смотреть полный таймлайн федеративной камеры в плеере СРВИ, чтобы обычные участки подтягивались из регионального архива, а заблокированные/изъятые/зарезервированные — из источников СРВИ и RFM.</span>

### 2.3. Администрирование и журнал

* **US.PPUV.3.1.** Как пользователь ППУВ на платформе СРВИ, я хочу просматривать историю и текущие блокировки с фильтрами и управлять ими из единого журнала блокировок, чтобы контролировать жизненный цикл операций ППУВ.
* **US.PPUV.3.2.** Как пользователь ППУВ на платформе СРВИ, я хочу менять статус своих активных блокировок через журнал блокировок, чтобы точечно управлять процессом.
  <span style="color:red;">Журнал должен поддерживать действия "Снять" и "Продлить" для активных блокировок; завершённая блокировка остаётся одной строкой со сменившимся статусом, а не превращается в набор отдельных событий.</span>
* **US.PPUV.3.3.** Как системный администратор, я хочу назначать ролям права ППУВ (приоритет управления, блокировка камеры, блокировка архива, перехват чужих блокировок) в Resource Management своей платформы, чтобы разграничить полномочия без изменения кода.
* **US.PPUV.3.4.** Как администратор системы, я хочу, чтобы при отсутствии лицензии PPUV весь связанный UI ППУВ был скрыт, чтобы пользователи не видели недоступный функционал.
* <span style="color:red;">**US.PPUV.3.5.** Как администратор инсталляции, я хочу настраивать максимальную длительность ручных блокировок через env/config, чтобы адаптировать лимит под требования заказчика без изменения кода.</span>

### 2.4. Особенности работы платформы

* **US.PPUV.4.1.** Как пользователь ППУВ на платформе СРВИ, я хочу, чтобы мои действия не фиксировались в аудите региональной платформы при сохранении полной записи в аудите СРВИ, чтобы сохранять тайну операции для гражданского контура.
* **US.PPUV.4.2.** Как пользователь ППУВ на платформе СРВИ, я хочу видеть происхождение камеры (локальная СРВИ, регион A, регион B) и заблокированные интервалы на таймлайне, чтобы однозначно понимать источник данных при работе с федеративными камерами.
* <span style="color:blue;">**US.PPUV.4.3.** Как разработчик/администратор федерации, я хочу иметь явную настройку доверенных региональных контуров и их идентификаторов, чтобы RSM СРВИ не путал камеры разных регионов и корректно маршрутизировал S2S-команды.</span>

---

## 3. Сценарии использования (Use Cases)

### UC-PPUV-01: Блокировка камеры

| **Наименование** | **UC-PPUV-01: Блокировка камеры (режим спецоперации / Spoofing)** |
| --- | --- |
| **Актор** | Пользователь ППУВ на платформе СРВИ; гражданский оператор на региональной платформе или СРВИ (пассивно) |
| **Предусловия** | 1. Пользователь авторизован на **СРВИ**.<br>2. Активна лицензия PPUV и право на блокировку камеры (RM).<br>3. UI подписан на события RSM (WebSocket/SSE).<br>4. Камера доступна в каталоге (локальная или проекция региона).<br><span style="color:blue;">5. Для федеративной камеры в СРВИ известны `resourceId`, `externalId` и `regionalInstallationId`.</span> |
| **Постусловия** | Блокировка (SPOOFING-lease) активна на период TTL; в мини-режиме — закрытый замок и таймер; у ППУВ — реальный live и запись на стримере СРВИ; у гражданских — заглушка/подмена и скрытый архив за период; в журнале блокировок СРВИ — записи Spoofing + Archive; Audit **СРВИ** содержит полную запись, Audit региона — **не содержит** следов ППУВ. <span style="color:blue;">После завершения блокировки фрагмент сохраняется в RFM и остаётся доступен на таймлайне СРВИ.</span> |
| **Основной сценарий** | 1. Пользователь открывает карту, список камер или галерею на платформе СРВИ.<br>2. Система отображает перечень камер, а также фильтр по системам-источникам.<br>3. Пользователь накладывает фильтр по источнику.<br>4. Система отображает только камеры выбранного источника.<br>5. Пользователь выбирает одну из камер.<br>6. Система отображает данные по системе-источнику в описании камеры.<br>7. Система запрашивает `POST /gateway/rsm/locks/status` по выбранному `resourceId`.<br>8. RSM СРВИ возвращает активные lease.<br>9. Система отображает на превью: закрытый замок + таймер — для заблокированной камеры; открытый замок — для свободной. <span style="color:red;">Для заблокированной камеры также отображается явное текстовое/иконографическое объяснение состояния, а не только красная рамка.</span><br>10. Пользователь нажимает на иконку замка.<br>11. Система проверяет его права:<br>&nbsp;&nbsp;11.1. Если это админ ППУВ или автор блокировки, переход на альтернативный сценарий 1.<br>&nbsp;&nbsp;11.2. Если нет, система сообщает, что пользователю запрещено снимать блокировку. Иконка не меняется.<br>12. Пользователь нажимает на иконку замка у свободной камеры (`lockType=SPOOFING`).<br>13. Система открывает выбор пресета TTL. <span style="color:red;">Выбор/ввод длительности не позволяет указать значение больше максимума из конфигурации; дефолтный максимум — 3 дня.</span><br>14. Пользователь выбирает период и подтверждает.<br>15. Система отправляет `POST /gateway/rsm/locks` (`lockType=SPOOFING`).<br>16. RSM создаёт Spoofing-lease и **автоматически** сопутствующий Archive-lease (`coupledFromLeaseId`). <span style="color:red;">Отдельный PTZ-lease при этом не создаётся.</span><br>17. <span style="color:blue;">Для федеративной камеры RSM по `regionalInstallationId` отправляет команду в нужный региональный RSM; регион применяет логическое скрытие для гражданских, а СРВИ запускает собственную запись периода блокировки.</span><br>18. RSM фиксирует операцию в Audit **СРВИ**.<br>19. Спецпользователь видит **реальный** live, иконку замка с таймером, границы заблокированного интервала на таймлайне.<br>20. UI региона получает `SpoofingStateChanged`.<br>21. Гражданский: камера **исчезает** из раскладки с заменой на случайную онлайн-камеру, если feature flag включён, или в одиночном просмотре показывает "камера не работает"; архив и аналитика за период **скрыты**.<br>22. По событиям `LockExpired` / `LockRevoked` / `SpoofingStateChanged` UI обновляется без перезагрузки страницы.<br>23. ППУВ снимает блокировку или истекает TTL.<br>24. <span style="color:blue;">Система инициирует сохранение фрагмента блокировки в RFM СРВИ и связывает его с таймлайном камеры.</span> |
| **Альтернативный сценарий 1. Снятие блокировки владельцем** | После п. 11.1 основного сценария или окончания TTL.<br>1. Система отправляет `DELETE /gateway/rsm/locks/{leaseId}` (или release по API).<br>2. RSM выполняет release; для федеративной — S2S "spoofing stop", восстановление live на регионе.<br>3. У гражданского пользователя live восстановлен; архив за период операции остаётся недоступен, если сохранена/действует архивная блокировка.<br>4. Администратор СРВИ в Audit СРВИ видит полную цепочку; администратор региона в Audit региона не обнаруживает операцию. |
| **Альтернативный сценарий 2. Локальная камера СРВИ** | 1. S2S **не выполняется**; блокировка применяется только в контуре СРВИ.<br>2. Гражданский оператор **СРВИ** (без роли ППУВ) видит заглушку по тем же правилам, что и на регионе. |
| **Исключающий сценарий 1. Ошибка S2S** | 1. Регион не подтверждает команду.<br>2. Lease переходит в статус "Ошибка"; ППУВ видит сообщение; гражданский контур **не меняется**.<br>3. Audit СРВИ фиксирует неуспешную попытку. |

### UC-PPUV-02: Перехват управления PTZ федеративной камеры

| **Наименование** | **UC-PPUV-02: Перехват управления PTZ федеративной камеры** <span style="color:red;">/ пользовательская операция "Блокировка PTZ"</span> |
| --- | --- |
| **Актор** | Пользователь ППУВ на платформе СРВИ |
| **Предусловия** | 1. Пользователь авторизован на **СРВИ**.<br>2. Гражданский оператор на **региональной платформе** управляет PTZ этой камеры.<br><span style="color:blue;">3. СРВИ знает регион-источник камеры и внешний идентификатор для S2S-команды.</span> |
| **Постусловия** | PTZ-управление у ППУВ на СРВИ; у гражданского на регионе джойстик отключён; live и архив доступны обоим. |
| **Основной сценарий** | 1. Спецпользователь на СРВИ открывает проекцию региональной PTZ-камеры.<br>2. Система отображает кнопку управления камерой. <span style="color:red;">Если на камере уже активна блокировка потока, действие "Блокировка PTZ" недоступно и сопровождается пояснением.</span><br>3. Пользователь нажимает кнопку <span style="color:red;">"Блокировка PTZ"</span>.<br>4. Система отправляет `POST /gateway/rsm/locks` (`lockType=PTZ`, `resourceId`). <span style="color:red;">TTL проверяется по общему максимуму, дефолт — 3 дня.</span><br>5. RSM СРВИ определяет `externalId` и `regionalInstallationId`.<br>6. RSM сравнивает приоритеты и выполняет preemption.<br>7. RSM СРВИ отправляет S2S-команду в RSM региона: сброс PTZ-сессии гражданского оператора.<br>8. DCS региона сбрасывает сессию; UI региона показывает уведомление гражданскому.<br>9. RSM СРВИ создаёт PTZ-lease; публикует `LockGranted` в Kafka.<br>10. Система активирует джойстик ППУВ на СРВИ.<br>11. Запись в Audit **СРВИ**; в Audit региона — **нет** следа операции ППУВ. |
| **Альтернативный сценарий 1. Равный приоритет** | 1. На шаге 6 RSM видит, что камера занята пользователем с **равным** `ppuv_priority`.<br>2. RSM возвращает `REJECTED` / `409 RESOURCE_BUSY`.<br>3. Система показывает сообщение: ресурс занят; джойстик не активируется. |
| <span style="color:red;">**Альтернативный сценарий 2. PTZ уже заблокирован, затем блокируется поток**</span> | <span style="color:red;">1. На камере активен PTZ-lease.<br>2. Пользователь с правами включает блокировку потока.<br>3. RSM создаёт Spoofing/Archive lease по UC-PPUV-01; существующий PTZ-lease не обязан автоматически сниматься.<br>4. После снятия блокировки потока PTZ-lease продолжает действовать до своего TTL или ручного снятия, если он не был снят отдельно.</span> |
| **Исключающий сценарий 1. Ошибка S2S** | 1. На шаге 7 регион не отвечает или возвращает ошибку.<br>2. RSM СРВИ не подтверждает lease (статус "Ошибка").<br>3. Система показывает ошибку оператору; PTZ у гражданского может остаться активным до повторной попытки. |

### UC-PPUV-03: Массовая блокировка камер на карте (лассо)

|  |  |
| --- | --- |
| **Наименование** | **UC-PPUV-03: Массовая блокировка камер на карте (лассо)** |
| **Актор** | Пользователь ППУВ на платформе СРВИ |
| **Предусловия** | 1. Пользователь авторизован на СРВИ.<br>2. На карте отображены камеры с координатами (локальные и проекции регионов). |
| **Постусловия** | Успешно заблокированные камеры отмечены иконкой замка; в журнале СРВИ — записи lease; для федеративных — применены S2S-команды в регионы. <span style="color:red;">Замок отображается только на одиночных камерах, не на агрегированных кластерах.</span> |
| **Основной сценарий** | 1. Спецпользователь на карте СРВИ рисует полигон.<br>2. Спецпользователь нажимает **"Заблокировать выбранные"**.<br>3. Система открывает модальное окно с пресетами TTL. <span style="color:red;">Пользователь выбирает тип массовой операции: блокировка потока или блокировка PTZ.</span><br>4. Пользователь выбирает период и подтверждает.<br>5. <span style="color:red;">Фронт отправляет геометрию области, тип операции и TTL; бэкенд определяет список камер внутри области, включая камеры, скрытые в кластерах.</span><br>6. Система определяет количество камер:<br>&nbsp;&nbsp;6.1. Если камер ≤ 50, система отправляет/обрабатывает `POST /gateway/rsm/locks/batch` (SPOOFING или PTZ).<br>&nbsp;&nbsp;6.2. Если камер > 50, система выводит сообщение: "Достигнут лимит в 50 камер для блокировки. Уменьшите количество и повторите операцию".<br>7. RSM обрабатывает **покамерно**: acquire / reject / preempt.<br>8. Для федеративных камер RSM отправляет S2S в соответствующие регионы.<br>9. Система показывает сводку: "Заблокировано N, отклонено M, вытеснено K".<br>10. Система вносит данные в аудит на платформе СРВИ.<br>11. Блокировка каждой отдельной камеры происходит аналогично UC-PPUV-01 или UC-PPUV-02. |
| **Альтернативный сценарий 1. Массовая разблокировка** | 1. Спецпользователь выделяет на карте область или выбирает записи в журнале блокировок.<br>2. Спецпользователь нажимает **"Снять блокировку"**.<br>3. Система формирует список `leaseId`:<br>&nbsp;&nbsp;3.1. Если пользователь без прав `admin:ppuv`, определяется перечень камер, где пользователь — владелец блокировки.<br>&nbsp;&nbsp;3.2. Если пользователь с правом `admin:ppuv`, используется полный перечень камер.<br>4. Система отправляет `POST /gateway/rsm/locks/batch/release`.<br>5. RSM снимает lock покамерно; для федеративных — S2S "spoofing stop" в регион.<br>6. Система показывает "Снято N из M" (partial success при чужих lock в области). |

### UC-PPUV-04: Логическая блокировка архива

|  |  |
| --- | --- |
| **Наименование** | **UC-PPUV-04: Логическая блокировка архива (без изъятия)** |
| **Актор** | Пользователь ППУВ на платформе СРВИ |
| **Предусловия** | 1. Пользователь авторизован на СРВИ с правом блокировки архива.<br>2. На таймлайне есть записи за выбранный интервал.<br><span style="color:blue;">3. Для федеративной камеры СРВИ может получить региональные DVR/HLS-сегменты или инициировать выгрузку файла через S2S.</span> |
| **Постусловия** | У гражданских — дыра на таймлайне; у ППУВ — полный доступ; при снятии lock доступ гражданским **восстанавливается**. |
| **Основной сценарий** | 1. ППУВ открывает таймлайн камеры (локальной или проекции региона) на СРВИ.<br>2. Пользователь выделяет интервал `[startTime, endTime]`.<br>3. Система предлагает варианты:<br>&nbsp;&nbsp;- Скрыть архив<br>&nbsp;&nbsp;- Изъять архив<br>&nbsp;&nbsp;- Сохранить копию<br>4. Пользователь выбирает **"Скрыть архив"**.<br>5. Система отправляет `POST /gateway/rsm/locks` (`lockType=ARCHIVE`).<br>6. RSM создаёт Archive-lease; для федеративной камеры — S2S в регион.<br>7. MSS региона помечает интервал как `BlockedArchive` для гражданских.<br>8. Гражданский на регионе видит **дыру** на таймлайне; live **не затронут**.<br>9. <span style="color:blue;">События аналитики и нотификации региона за интервал также скрываются от гражданского пользователя региона.</span><br>10. ППУВ видит архив полностью и границы скрытого интервала в плеере СРВИ.<br>11. ППУВ снимает блокировку из таймлайна или журнала.<br>12. Доступ гражданских к архиву **восстанавливается**. |
| **Альтернативный сценарий 1. Дублирование в спецархив** | После п. 3 основного сценария.<br>1. Пользователь нажимает **"Дублирование в спецархив"** / **"Изъять архив"**.<br>2. Система отправляет в RSM запрос Archive-lock и команду извлечения.<br>3. RSM СРВИ через S2S инициирует `ExtractArchiveToStorage` на регионе.<br>4. MSS региона выгружает файл с Nimble/медиастримера (не "перенос потока").<br>5. Файл загружается в **RFM СРВИ**.<br>6. Гражданский видит пустой/заблокированный интервал на таймлайне региона.<br>7. ППУВ видит копию в RFM и метку на таймлайне СРВИ.<br>8. Система вносит данные в аудит на платформе СРВИ. |
| **Альтернативный сценарий 2. Сохранить копию архива** | 1. Пользователь выбирает **"Сохранить копию"** без скрытия интервала.<br>2. Система через S2S получает файл из регионального архива и сохраняет его в RFM СРВИ.<br>3. Оригинальный архив и live региона остаются доступными гражданским пользователям.<br>4. <span style="color:blue;">Копия отображается в СРВИ как RFM-фрагмент, доступный из таймлайна камеры или из раздела сохранённых записей.</span> |
| **Исключающий сценарий 1. Пересечение архивов** | 1. Выделенный интервал частично пересекается с ранее скрытым / изъятым фрагментом.<br>2. Система блокирует операцию и показывает: "Нельзя выделить пересекающийся интервал". |

### UC-PPUV-05: Работа с журналом блокировок

|  |  |
| --- | --- |
| **Наименование** | **UC-PPUV-05: Работа с журналом блокировок** |
| **Актор** | Пользователь ППУВ на платформе СРВИ |
| **Связанные US** | US.PPUV.3.1, US.PPUV.3.2 |
| **Предусловия** | 1. Пользователь авторизован на **СРВИ** (на регионе раздел **отсутствует**). |
| **Постусловия** | Отображён актуальный список блокировок; при снятии — статус "Завершена". <span style="color:red;">При продлении меняется время окончания активной строки, а не создаётся новая строка блокировки.</span> |
| **Основной сценарий** | 1. Пользователь открывает **"Журнал блокировок"** в меню СРВИ.<br>2. Система запрашивает `GET /gateway/rsm/locks` с пагинацией.<br>3. Система отображает таблицу: камера (+ регион-источник), тип (PTZ/Spoofing/Archive/Reserve), статус, владелец, создание, истечение, кто снял. <span style="color:red;">В таблице должны отображаться читабельные данные камеры, а не только id ресурса.</span><br>4. Пользователь применяет фильтры (камера, инициатор, дата, статус, тип, <span style="color:blue;">регион-источник</span>, <span style="color:red;">только активные</span>).<br>5. Пользователь нажимает **"Снять"** у своей активной записи.<br>6. RSM выполняет release; статус → "Завершена", причина "Снята пользователем".<br>7. Система вносит данные в аудит платформы СРВИ. |
| **Альтернативный сценарий 2. Админ снимает чужую** | 1. Пользователь с `ppuv:admin` снимает чужой lease.<br>2. В поле "Кто снял" — admin; бывший владелец получает `LockRevoked`. |
| <span style="color:red;">**Альтернативный сценарий 3. Продление активной блокировки**</span> | <span style="color:red;">1. Пользователь выбирает свою активную блокировку или администратор выбирает доступную ему активную блокировку.<br>2. Пользователь нажимает "Продлить".<br>3. Система предлагает новую длительность/время окончания с учётом максимального TTL из конфигурации.<br>4. RSM обновляет lease и публикует событие изменения.<br>5. В журнале остаётся та же строка с обновлённым временем окончания и записью в Audit СРВИ.</span> |

### UC-PPUV-06: Резервирование архива федеративной камеры

<span style="color:blue;">Сценарий добавлен по итогам встречи: резервирование было проговорено отдельно от блокировки камеры и выборочной записи.</span>

|  |  |
| --- | --- |
| **Наименование** | **UC-PPUV-06: Резервирование архива федеративной камеры** |
| **Актор** | Пользователь ППУВ на платформе СРВИ |
| **Связанные US** | US.PPUV.2.4 |
| **Предусловия** | 1. Пользователь авторизован на СРВИ и имеет право резервирования.<br>2. Федеративная камера доступна в каталоге СРВИ.<br>3. RSM знает регион-источник и параметры подключения потока. |
| **Постусловия** | В контуре СРВИ ведётся собственная запись камеры; гражданский региональный контур работает без видимых изменений. |
| **Основной сценарий** | 1. Пользователь открывает федеративную камеру в СРВИ.<br>2. Пользователь нажимает **"Резервировать архив"**. <span style="color:red;">Эта кнопка не находится в одной таблице с блокировкой потока и блокировкой PTZ; она размещается в карточке камеры/плеере отдельным UX-решением.</span><br>3. Система предлагает выбрать период или включить бессрочно до ручного снятия.<br>4. Пользователь подтверждает.<br>5. RSM СРВИ создаёт Reserve-lease и инициирует подключение потока к медиастримеру СРВИ.<br>6. Региональная система продолжает показывать live, архив, события и нотификации гражданским пользователям без изменений.<br>7. В журнале блокировок/операций СРВИ появляется запись резервирования.<br>8. Пользователь СРВИ видит на таймлайне, какие интервалы доступны из собственного резерва СРВИ. |
| **Альтернативный сценарий 1. Снятие резервирования** | 1. Пользователь или администратор ППУВ снимает Reserve-lease.<br>2. RSM останавливает параллельную запись в контуре СРВИ.<br>3. Уже записанные фрагменты остаются доступны в соответствии с политикой хранения. |

### UC-PPUV-07: Просмотр полного таймлайна федеративной камеры

<span style="color:blue;">Сценарий добавлен по итогам встречи: отдельно зафиксировано, что СРВИ-пользователь должен работать с архивом через камеру/плеер СРВИ, а не через региональную платформу.</span>

|  |  |
| --- | --- |
| **Наименование** | **UC-PPUV-07: Просмотр полного таймлайна федеративной камеры в СРВИ** |
| **Актор** | Пользователь ППУВ на платформе СРВИ |
| **Связанные US** | US.PPUV.2.6, US.PPUV.4.2 |
| **Предусловия** | 1. Федеративная камера импортирована/спроецирована в СРВИ.<br>2. Для камеры есть региональный архив, резерв СРВИ и/или RFM-фрагменты. |
| **Постусловия** | Пользователь видит единый таймлайн камеры с отметками источников и заблокированных интервалов. |
| **Основной сценарий** | 1. Пользователь открывает камеру в СРВИ.<br>2. Система строит таймлайн по метаданным СРВИ и региона.<br>3. Для обычных интервалов СРВИ получает подписанную внешнюю ссылку или проксируемый ресурс регионального медиастримера.<br>4. Для заблокированных/изъятых/зарезервированных интервалов СРВИ отдаёт фрагменты со своего медиастримера или из RFM.<br>5. Плеер воспроизводит выбранный интервал как единый архив, хотя физические источники могут отличаться.<br>6. Пользователь видит происхождение камеры и границы заблокированных/RFM-фрагментов. |
| **Исключающий сценарий 1. Региональный архив недоступен** | 1. Региональный медиастример не отдаёт ссылку или сегменты.<br>2. Система показывает ошибку только для соответствующего интервала.<br>3. Доступные RFM/СРВИ-фрагменты продолжают отображаться. |

### UC-PPUV-09: Скрытность операций ППУВ в аудите (федерация)

|  |  |
| --- | --- |
| **Наименование** | **UC-PPUV-09: Скрытность операций ППУВ в аудите федерации** |
| **Актор** | Пользователь ППУВ на СРВИ; администратор региональной платформы |
| **Связанные US** | US.PPUV.4.1 |
| **Предусловия** | 1. ППУВ выполняет операцию над федеративной камерой (блокировка, PTZ, архив, <span style="color:blue;">резервирование или выборочная запись</span>). |
| **Постусловия** | Audit СРВИ содержит полную запись; Audit региона — **не содержит** следов ППУВ. |
| **Основной сценарий** | 1. ППУВ на СРВИ выполняет операцию над федеративной камерой.<br>2. RSM СРВИ фиксирует операцию в Audit **СРВИ**.<br>3. RSM СРВИ отправляет S2S в регион **без** пользовательского контекста ППУВ в гражданском аудите.<br>4. Гражданский оператор региона продолжает работу; в своём Audit **не находит** записей о блокировке/изъятии/резервировании.<br>5. Администратор СРВИ в Audit СРВИ **видит** полную цепочку действий ППУВ.<br>6. Администратор региона в Audit региона **не обнаруживает** операцию. |

---

## <span style="color:red;">4. Критерии приёмки</span>

<span style="color:red;">Раздел добавлен по итогам второй транскрипции и должен быть последним блоком документа. Критерии разделены по смысловым областям и покрывают backend и frontend.</span>

### <span style="color:red;">4.1. Конфигурация, права и лицензия</span>

* <span style="color:red;">Backend: при отсутствии лицензии PPUV API не выдаёт доступ к операциям ППУВ для пользователя без соответствующих прав; ошибки прав возвращаются предсказуемо и не создают lease.</span>
* <span style="color:red;">Frontend: при отсутствии лицензии или прав ППУВ элементы блокировки/журнала/резервирования скрыты или disabled с корректным пояснением согласно принятому UX.</span>
* <span style="color:red;">Backend: максимальный TTL ручных блокировок читается из env/config; значение по умолчанию — 3 дня.</span>
* <span style="color:red;">Frontend: UI выбора длительности не позволяет отправить TTL больше конфигурационного лимита независимо от того, вводится срок в минутах, часах или днях.</span>
* <span style="color:red;">Backend: при обходе фронтовой валидации запрос с TTL больше лимита отклоняется с понятной ошибкой.</span>

### <span style="color:red;">4.2. Блокировка потока/камеры</span>

* <span style="color:red;">Backend: `SPOOFING`/stream-lock создаёт lease блокировки потока и связанный archive-lock, но не создаёт отдельный `PTZ` lease.</span>
* <span style="color:red;">Backend: для федеративной камеры команда блокировки маршрутизируется в правильный регион по `regionalInstallationId` и `externalId`.</span>
* <span style="color:red;">Backend: при ошибке S2S lease не становится успешным, гражданский контур не меняется, ошибка фиксируется в Audit СРВИ.</span>
* <span style="color:red;">Frontend: заблокированная камера для оператора ППУВ имеет явный индикатор состояния: замок/подпись/таймер или другой согласованный элемент, а не только красную рамку.</span>
* <span style="color:red;">Frontend: после включения блокировки потока действие "Блокировка PTZ" для этой камеры недоступно и сопровождается пояснением, что управление уже недоступно из-за блокировки потока.</span>
* <span style="color:red;">Frontend: гражданский пользователь видит заглушку/подмену согласно feature flag и не видит архив/события за период блокировки.</span>

### <span style="color:red;">4.3. Блокировка PTZ</span>

* <span style="color:red;">Frontend: пользовательская операция называется "Блокировка PTZ"; термин "перехват управления" не используется как основное название кнопки/строки.</span>
* <span style="color:red;">Backend: `PTZ` lock создаётся отдельным lease с собственным TTL и не скрывает live/архив.</span>
* <span style="color:red;">Backend: при создании PTZ-lock текущая менее приоритетная PTZ-сессия сбрасывается через S2S/DCS, а гражданский оператор получает событие отключения управления.</span>
* <span style="color:red;">Backend: равный приоритет возвращает конфликт (`409 RESOURCE_BUSY` или эквивалент), не создавая новый lease.</span>
* <span style="color:red;">Frontend: если сначала создан PTZ-lock, пользователь с правом может позже включить блокировку потока; UI корректно показывает оба состояния согласно данным API.</span>
* <span style="color:red;">Frontend: PTZ-lock отображает оставшееся время и статус, а после истечения/снятия управление обновляется без перезагрузки страницы.</span>

### <span style="color:red;">4.4. Массовые операции и карта</span>

* <span style="color:red;">Frontend: массовая операция по лассо позволяет выбрать тип блокировки: поток/камера или PTZ.</span>
* <span style="color:red;">Frontend: при массовой операции отправляется геометрия области и параметры операции; фронт не обязан раскрывать кластеры и перечислять все cameraId внутри них.</span>
* <span style="color:red;">Backend: по геометрии области определяется набор камер, применяются лимиты количества и возвращается partial success: заблокировано, отклонено, вытеснено.</span>
* <span style="color:red;">Backend: лимит массовой операции проверяется на сервере; при превышении операция не приводит к частичному незаметному переносу сотен/тысяч потоков.</span>
* <span style="color:red;">Frontend: замок/индикатор блокировки отображается только на одиночных камерах, присутствующих в ответе карты; кластеры не получают агрегированный замок.</span>
* <span style="color:red;">Frontend: после раскрытия кластера или появления одиночной камеры статус блокировки запрашивается/обновляется и отображается на конкретной камере.</span>

### <span style="color:red;">4.5. Фильтры, каталог и федеративные источники</span>

* <span style="color:red;">Backend: СРВИ хранит локальный слепок федеративных камер с собственным `resourceId`, `externalId`, `regionalInstallationId`, названием, координатами и источником.</span>
* <span style="color:red;">Backend: изменения названия, координат и ключевых атрибутов внешней камеры синхронизируются в слепок СРВИ без ручного пересоздания камеры.</span>
* <span style="color:red;">Backend: фильтр по активной блокировке доступен через свойства камеры или агрегирующий endpoint, совместимый с картой, списком и галереей.</span>
* <span style="color:red;">Backend: теги и иерархии из разных источников не конфликтуют по одинаковым именам; источник/namespace учитывается при фильтрации.</span>
* <span style="color:red;">Frontend: пользователь может отфильтровать камеры по источнику (локальные СРВИ, регион A, регион B) и по признаку активной блокировки.</span>
* <span style="color:red;">Frontend: если у источника нет собственной иерархии, камеры отображаются в корневом узле этого источника.</span>
* <span style="color:red;">Frontend: фильтры источника и блокировки доступны на карте и в списочных представлениях; выбранные фильтры не ломают дальнейшую интеграцию Finder.</span>

### <span style="color:red;">4.6. Журнал блокировок</span>

* <span style="color:red;">Backend: `GET /gateway/rsm/locks` возвращает не только id, но и данные, достаточные для читабельной строки журнала: камера, источник, тип, статус, владелец, начало, окончание, кто снял.</span>
* <span style="color:red;">Backend: блокировка при снятии или истечении меняет статус существующей записи; новая строка "снятия" не создаётся как отдельная блокировка.</span>
* <span style="color:red;">Backend: продление активной блокировки обновляет время окончания существующего lease, проверяет права и TTL-лимит, пишет Audit СРВИ и публикует событие обновления.</span>
* <span style="color:red;">Frontend: журнал поддерживает фильтры по статусу, типу, инициатору, камере, источнику и режим "только активные".</span>
* <span style="color:red;">Frontend: для активных блокировок доступны действия "Снять" и "Продлить" с учётом прав пользователя; недоступные действия disabled или скрыты.</span>
* <span style="color:red;">Frontend: массовое продление/снятие через журнал удобно для сценария, когда область на карте повторно выделить сложно.</span>

### <span style="color:red;">4.7. Архив, RFM и резервирование</span>

* <span style="color:red;">Backend: при блокировке потока период блокировки записывается/сохраняется в контуре СРВИ и после завершения попадает в RFM согласно политике хранения.</span>
* <span style="color:red;">Backend: резервирование создаёт собственную запись СРВИ без скрытия live/архива/событий на регионе.</span>
* <span style="color:red;">Backend: выборочная запись исторического интервала сохраняет файл в RFM без удаления оригинала на региональном медиастримере.</span>
* <span style="color:red;">Frontend: RFM-фрагменты и региональные интервалы отображаются оператору СРВИ как единый таймлайн камеры с понятными отметками источника.</span>
* <span style="color:red;">Frontend: управление резервированием не размещается третьей строкой в модальном окне блокировки потока/PTZ; используется отдельный элемент управления в карточке/плеере после UX-решения.</span>

### <span style="color:red;">4.8. Аудит, события и скрытность</span>

* <span style="color:red;">Backend: все успешные и неуспешные операции ППУВ над федеративными камерами пишутся в Audit СРВИ.</span>
* <span style="color:red;">Backend: региональный гражданский Audit не содержит пользовательского контекста и следов ППУВ-операций СРВИ.</span>
* <span style="color:red;">Backend: события `LockGranted`, `LockRevoked`, `LockExpired`, `LockUpdated` или их эквиваленты публикуются так, чтобы UI обновлял статусы без перезагрузки.</span>
* <span style="color:red;">Frontend: гражданский пользователь региона не видит события аналитики и нотификации за заблокированный интервал.</span>
* <span style="color:red;">Frontend: оператор СРВИ не видит региональную аналитику как агрегированную аналитику СРВИ, но видит служебные статусы ППУВ по камере.</span>

### <span style="color:red;">4.9. Совместимость с будущими расписаниями</span>

* <span style="color:red;">Backend: ручные endpoints acquire/release/update lease пригодны для вызова будущим сервисом расписаний без отдельной ветки логики в RSM.</span>
* <span style="color:red;">Backend: модель lease содержит достаточно данных, чтобы в будущем расписание могло создавать блокировки потока и PTZ по тем же правилам прав, TTL и аудита.</span>
* <span style="color:red;">Frontend: текущий UI не обязан реализовывать расписания, но терминология и структура журнала не должны конфликтовать с будущими запланированными блокировками.</span>
